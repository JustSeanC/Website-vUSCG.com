---
deployment:
  tasks:
    - export DEPLOYPATH=/home/vuscgco1/public_html
    - /bin/mkdir -p "$DEPLOYPATH"
    - '/usr/bin/rsync -av --exclude ".git/" --exclude ".github/" --exclude ".cpanel.yml" --exclude "README*" --exclude "LICENSE*" --exclude "*.zip" --exclude "error_log" --exclude "prepro/" --exclude ".well-known/" ./ "$DEPLOYPATH"/'